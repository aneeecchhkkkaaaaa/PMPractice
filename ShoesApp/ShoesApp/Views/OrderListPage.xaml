<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ShoesApp.OrderListPage"
             Title="Заказы"
             BackgroundColor="#F5F5F5">

	<Grid RowDefinitions="Auto, *" Padding="10">
		<Button Grid.Row="0"
                Text="Добавить заказ"
                Command="{Binding AddOrderCommand}"
                IsVisible="{Binding CanManage}"
                BackgroundColor="#4CAF50"
                TextColor="White"
                FontAttributes="Bold"
                HeightRequest="40"
                CornerRadius="5"
                HorizontalOptions="Center"
                Margin="0,0,0,10"/>

		<CollectionView Grid.Row="1" ItemsSource="{Binding Orders}">
			<CollectionView.ItemTemplate>
				<DataTemplate>
					<Border Margin="0,5" Padding="10" Stroke="Black" StrokeThickness="1" BackgroundColor="White">
						<Grid RowDefinitions="Auto, Auto">
							<Grid Grid.Row="0" ColumnDefinitions="*, Auto">
								<VerticalStackLayout Grid.Column="0" Spacing="3">
									<Label Text="{Binding OrderId, StringFormat='Артикул заказа: {0}'}" FontAttributes="Bold"/>
									<Label Text="{Binding Status.OrderStatusName, StringFormat='Статус: {0}'}"/>
									<Label Text="{Binding Address.AddressId, StringFormat='Адрес пункта выдачи: {0}'}"/>
									<Label Text="{Binding OrderDate, StringFormat='Дата заказа: {0:dd.MM.yyyy}'}"/>
								</VerticalStackLayout>

								<VerticalStackLayout Grid.Column="1" HorizontalOptions="Center" VerticalOptions="Center" Spacing="5" Margin="10,0,0,0">
									<Label Text="Дата доставки" FontAttributes="Bold" FontSize="14"/>
									<Label Text="{Binding DeliveryDate, StringFormat='{0:dd.MM.yyyy}'}" FontSize="16" FontAttributes="Bold"/>
								</VerticalStackLayout>
							</Grid>

							<Grid Grid.Row="1" ColumnDefinitions="*, *" Margin="0,8,0,0"
                                  IsVisible="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.CanManage}">
								<Button Grid.Column="0" Text="Редактировать"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditOrderCommand}"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="#2E8B57" TextColor="White" FontSize="12" HeightRequest="30" Margin="0,0,5,0"/>
								<Button Grid.Column="1" Text="Удалить"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteOrderCommand}"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="#FF4D4D" TextColor="White" FontSize="12" HeightRequest="30" Margin="5,0,0,0"/>
							</Grid>
						</Grid>
					</Border>
				</DataTemplate>
			</CollectionView.ItemTemplate>
		</CollectionView>
	</Grid>
</ContentPage>